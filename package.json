{
  "name": "hss-drive-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @hss/web dev",
    "dev:api": "pnpm --filter @hss/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
     "typescript": "^5.4.5"
  }
}
